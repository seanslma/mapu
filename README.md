# mapu

My python utils package. It includes some commonly used functions, like `gen_rand_df` and `explode_date_range`.

## How to install `mapu`

Using `pip`:
```sh
pip install mapu
```

Using `uv`:
```sh
uv pip install mapu
```

## The document for `mapu`

Visit the [document](https://seanslma.github.io/mapu) for more details.
